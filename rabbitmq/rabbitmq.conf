# Block publishers at 90% RAM usage (lazy queues keep data on disk)
vm_memory_high_watermark.relative = 0.9

# Never block until disk < 1 GB free
disk_free_limit.absolute = 1073741824

# Kill consumers that don't ack within 1 hour
consumer_timeout = 3600000

